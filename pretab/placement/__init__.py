"""Placement subsystem: where basis units go and how many there are.

A single home for location + resolution logic shared by splines, feature maps,
PLE and periodic encoders. Populated during the 1.0.0 restructure (Phase 2):
``base``, ``unsupervised``, ``supervised``, ``resolution``, ``adapters`` and
``factory`` modules.
"""
